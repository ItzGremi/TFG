<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>JoClock - Cambiar Contraseña</title>
    <link href='https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css' rel='stylesheet'>
    <link rel="stylesheet" href="/css/iniciosesion.css">
    <script defer src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
</head>
<body>
    
    <div class="wrapper">
        <form action="/cambiar-contra" method="POST">
            <h1>Cambiar Contraseña</h1>
            <div class="input-box">
                <i class='bx bxs-lock-alt' style='color:#fffdfd' ></i>
                <input type="password" name="pass" placeholder="Nueva Contraseña">
            </div>
            <div class="input-box">
                <i class='bx bxs-lock-alt' style='color:#fffdfd' ></i>
                <input type="password" name="confirmarpass" placeholder="Confirmar Nueva Contraseña">
            </div>

            <button type="submit" class="btn">Cambiar Contraseña</button>

            <div class="register-link">
                <p>¿Ya tienes una cuenta? <a href="iniciosesion">Iniciar sesión</a> </p>
            </div>
        </form>

    </div>

    <% if(typeof alert != "undefined") { %>
        <script>
            Swal.fire({
                title: '<%= alertTitle%>',
                text: '<%= alertMessage%>',
                icon: '<%= alertIcon%>',
                showConfirmButton: <%= showConfirmButton%>,
                timer: <%= timer%>
            }).then(()=>{
                window.location='/<%=ruta%>'
            })
        </script>
    <% } %>

</body>
</html>
